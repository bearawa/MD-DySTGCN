# 复现差距诊断与改进记录

## 1. 现象

| 来源 | 宗关 RMSE (z-space) | MAPE |
|------|---------------------|------|
| 论文表5-5 MD-DySTGCN | **0.2649** | **7.67%** |
| 论文表5-5 PatchTST | 0.2912 | — |
| 初版 1-epoch 冒烟 | 0.522 | 10.77% |
| 改进前完整训练 (modeltrain) | ≈0.52 | ≈8.8% |
| **改进后最佳 (v3)** | **0.427** | **8.05%** |
| 持续性基线 (重复最后观测) | 0.468 | 8.83% |

## 2. 根因（按重要性）

### 2.1 原复现几乎没超过「持续性」基线
在相同测试集上，把输入最后一步水质直接复制 12 步，RMSE 已达 **0.468**。  
原 v2 完整训练 ≈0.52，说明模型几乎在「学恒等映射」附近徘徊，而不是学到显著的动态残差。

### 2.2 仅训练 1 epoch / 早停选错指标会放大差距
冒烟实验只有 1 epoch；且用 Huber loss 早停时，val RMSE 并非最优。这解释了「和论文差一倍」的一部分，但不是全部。

### 2.3 论文 RMSE 与 MAPE 的可对照性不一致
改进后 **MAPE 已接近论文**（8.05% vs 7.67%），但 z-RMSE 仍高约 61%。  
物理量纲上，浊度 RMSE≈8.7 NTU，甚至优于论文消融中给出的 14.06 NTU。  
→ 更像是「标准化空间全局 RMSE」的聚合方式 / 数据难度与论文不完全一致，而不是单纯「模型完全无效」。

### 2.4 论文暗示使用未来 48h 气象，原稿未建模
§5.1 写明 48h 气象预报置信度高。原实现解码器只看历史 Tin，未注入未来 Tout 气象。  
v6 注入未来气象后 RMSE≈0.427（与 v3 持平），MAPE 略好到 7.94%，说明有帮助但不是主缺口。

### 2.5 论文未公开的实现细节
隐藏维 F、L_g、是否通道独立、Z-Score 按全局还是按站点、评估是否含全部 horizon 等，原文未给全。  
单站点通道独立基线在本数据上也难以打到论文 PatchTST 的 0.29，暗示**数据预处理或评估协议可能存在差异**。

## 3. 已落地的改进

`train_improved.py` / `outputs/model_runs_v3/`（**最佳论文可比结果**）：

1. **残差解码**：预测 Δ，再加最后观测  
2. **水质塔通道独立 TCN**（对齐 PatchTST 思路）  
3. **时间 last+mean 池化**  
4. **扩大 TCN 感受野**（L_tcn=6）  
5. **按 val 宗关 RMSE 早停**  
6. **宗关 loss 加权**

其它实验：`model_runs_v4`（按站点 Z-score）、`v5`（双路径）、`v6`（未来气象）— MAPE 可到 ~7.9%，RMSE 未优于 v3。

## 4. 结论

- 相对初版：RMSE **0.52 → 0.43**，MAPE **10.8% → 8.05%**，已稳定超过持续性基线。  
- 相对论文 RMSE 0.26：在当前公开数据与预处理下，**难以无歧义地复现到该数值**；MAPE 已基本对齐。  
- 若要继续逼近 0.26，优先核对：作者原始预处理脚本、Z-Score 协议、是否泄漏未来信息、以及表5-5 的精确聚合代码。

## 5. 复现命令

```bash
cd /root/cursor
python train_improved.py   # -> outputs/model_runs_v3/
# 最佳权重也复制到 outputs/model_runs/md_dystgcn_best_improved.pt
```
